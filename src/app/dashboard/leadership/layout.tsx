import { LeadershipLayout } from "@/features/leadership/components/leadership-layout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <LeadershipLayout>{children}</LeadershipLayout>;
}
